<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999xhtml">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="keywords" content="{{meta.tags}}" />
    <meta name="description" content="{{meta.description}}" />
    <link rel="stylesheet" href="{{paths.CSS}}/reset.css">
    <link rel="stylesheet" href="{{paths.CSS}}/main.css">
    <link rel="stylesheet" href="{{paths.CSS}}/post.css">
    <title>{{meta.title}}</title>
</head>
<body>
    <h1>{{meta.title}}</h1>
    <h2>Category: {{meta.category}}</h2>
    <div>{{{content}}}</div>
</body>
</html>